# 企业管理工作台 · 2.0 开发文档

版本：v2.0-planning · 更新日期：2026-07-20
配套 1.0 文档：`企业管理工作台开发文档.md`
配套进度文档：`项目进度与完成度.md`

> 本文只记录 1.0 未实现项与 2.0 新增功能，不重复描述 1.0 已完成的认证、日报、红绿灯、OKR 基础 CRUD、订阅基础能力、个人主页、企业设置和 AI 评分基础能力。

---

## 1. 当前检查结论

| 需求 | 当前项目状态 | 2.0 处理 |
|---|---|---|
| OKR 重要度 `o1/o2/o3`，按重要度排序，可拖动 | 未完成。当前 `okr_objectives` 只有 `sort_order`，无重要度字段；前端 OKR 卡片无拖拽排序交互 | 纳入 2.0 新增功能 |
| 个人主页评分仅展示分数和评价，不出现日报、OKR 详细数据 | 已完成。`people_service` 仅返回 `score`、`summary`、`updated_at`；前端评分卡仅渲染分数和摘要 | 不作为 2.0 开发项，仅作为隐私验收约束保留 |
| 订阅权限进一步设计，补齐隐私设计 | 部分完成。1.0 已实现任务级 `is_private`：owner/明确协作者可见，普通订阅者不可见；仍无被订阅人审批、拒绝名单和授权审计策略 | 保留现有私人任务约束，审批与审计纳入 2.0 核心设计 |

---

### 1.1 已落地的非回归约束

- 私人日报任务不能出现在普通 follower 的订阅日报、AI 上下文或个人主页日报日历中；任务 owner 和明确协作者仍可访问。派发发起人的自动订阅不构成私人任务授权。
- 私人属性只作用于任务，同一日期的公开任务仍可在订阅日报中展示。问题与解决方案当前没有私人字段，继续按整份日报只读返回；2.0 若需要问题级隐私，必须单独设计字段和迁移，不能沿用任务过滤作隐式假设。
- 主题强调色覆盖顶栏及业务模块的激活导航/Tab、今天日期、通用选择态、输入焦点、普通按钮、可点击文字、功能图标和进度条；新增普通交互禁止直接使用固定 `blue-*`，统一读取 `--theme-accent` 系列变量。
- 红绿灯红/黄/绿、成功/警告/错误、AI 分类、协作身份、同步运行状态和品牌 Logo 等语义/品牌色不随主题变化。
- 字体大小设置必须同步所有固定高度容器。当前日报周/月卡片和双行顶栏已联动紧凑/标准/舒展尺寸，后续新增固定高度组件时需要在三档字号下做无裁切、无重叠回归。
- 文字会随根字号缩放的组件不得继续使用不联动的固定像素高度；日期卡、标题栏、任务行和 Tab 采用 `rem` 或主题尺寸变量。导航/Tab 激活线只能放在自身边界内，禁止用负 `bottom` 跨过分隔线；所有可交互按钮/链接的 `focus-visible` 必须使用主题色。

---

## 2. 2.0 范围

### 2.1 P0：1.0 收口必补

这些是 1.0 已有模块的缺口，优先在 2.0 第一轮补齐。

1. OKR 重要度与拖拽排序。
2. 订阅权限与隐私设计。
3. 订阅聚合流：把订阅日报、订阅 OKR 的更新入口整理成更易浏览的聚合视图。
4. 企业设置审计日志：记录企业名称、Logo、页脚等变更人、时间和旧值摘要。

已完成并从 P0 移除：

- 后台同步日志列表（1.0 已落地：`/admin/sync-history` + `contact_sync_logs` 列表接口）。

### 2.2 P1：2.0 新增体验

1. 通知中心。
2. 灵感清单。
3. 更完整的企业品牌设置：登录页说明、页脚链接、默认头像/默认 Logo、品牌色。
4. 订阅通知：被订阅、订阅申请、订阅审批、对方日报/OKR 更新提醒。

### 2.3 P2：后续上线能力

AI 大脑聊天、AI 智能体、Claude 额度授权、模型额度统计等 AI 扩展能力不阻塞 1.0/2.0 后端使用，可作为后续上线内容独立排期。

---

## 3. OKR 重要度与排序

### 3.1 目标

OKR Objective 增加重要度，用于表达目标优先级，并影响默认展示顺序：

| 数据值 | 前端文案 | 排序优先级 |
|---|---|---:|
| `o1` | O1 最高优先级 | 1 |
| `o2` | O2 重要目标 | 2 |
| `o3` | O3 常规目标 | 3 |

默认排序规则：

```text
importance asc -> sort_order asc -> id asc
```

也就是 O1 排在 O2 前，O2 排在 O3 前；同一重要度内再按拖拽顺序展示。

### 3.2 数据模型

新增字段：

| 表 | 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|---|
| `okr_objectives` | `importance` | varchar(10) | `o2` | 只允许 `o1/o2/o3` |

保留现有 `sort_order`，用于同一重要度下的手动排序。

迁移策略：

- 既有 Objective 默认回填为 `o2`。
- 增加服务层校验，拒绝非 `o1/o2/o3` 值。
- 查询排序从 `sort_order, id` 调整为 `importance_rank, sort_order, id`。

### 3.3 API

调整现有接口，不新增独立资源：

| 方法 | 路径 | 调整 |
|---|---|---|
| GET | `/api/v1/okr?month=YYYY-MM` | Objective 返回 `importance` |
| POST | `/api/v1/okr/objectives` | 支持传入 `importance`，默认 `o2` |
| PATCH | `/api/v1/okr/objectives/{id}` | 支持修改 `importance` 与 `sort_order` |
| PATCH | `/api/v1/okr/objectives/reorder` | 批量提交同月 Objective 的 `id + importance + sort_order` |

批量排序接口建议请求体：

```json
{
  "month": "2026-07",
  "items": [
    { "id": 101, "importance": "o1", "sort_order": 1 },
    { "id": 102, "importance": "o1", "sort_order": 2 },
    { "id": 103, "importance": "o2", "sort_order": 1 }
  ]
}
```

### 3.4 前端交互

- Objective 卡片头部显示重要度 badge。
- 新建/编辑 Objective 时可选择重要度。
- OKR 列表按 O1/O2/O3 排序展示。
- 支持拖拽排序：
  - 同一重要度内拖动，只更新 `sort_order`。
  - 跨重要度拖动，同时更新 `importance` 与目标重要度组内 `sort_order`。
- 拖拽建议使用 `dnd-kit`，主开发文档已把它列为可选技术栈。

### 3.5 验收

- 新建 Objective 不传重要度时默认为 `o2`。
- O1 始终显示在 O2/O3 前。
- 同一重要度内拖动后刷新页面顺序保持一致。
- 跨组拖动后，目标的重要度与排序同时落库。
- 订阅 OKR 只读视图使用同样排序，不提供拖拽。
- AI OKR 质量评分读取 OKR 时按同样顺序组装上下文。

---

## 4. 个人主页评分隐私约束

### 4.1 当前结论

该项 1.0 已满足，不需要新增开发任务。

当前个人主页评分接口只返回：

- `status`
- `score`
- `summary`
- `updated_at`

前端只展示：

- 分数
- 评价摘要
- 生成日期

不会展示日报任务、问题解决方案、OKR Objective、KR、月报正文等详细数据。

### 4.2 2.0 非回归约束

后续改个人主页、订阅隐私或 AI 评分时必须保持：

- 个人主页评分区不返回原始日报、问题、OKR、月报正文。
- 评分摘要可直接查看，不要求订阅。
- 订阅关系只控制日报/OKR 明细视图，不控制个人主页评分摘要。
- 如果未来增加“评分详情”，也只能展示维度分、等级、摘要、建议这类 AI 输出，不得混入原始业务明细。

---

## 5. 订阅权限与隐私设计

### 5.1 当前问题

1. 当前订阅是主动订阅模型：A 订阅 B 后，只要 B active，即可读取 B 的 OKR 和日报中的公开任务；`is_private=true` 的私人任务会在服务端过滤。
2. 被订阅人没有审批、拒绝、关闭、可见范围控制。
3. 数据层只有 `daily_enabled` / `okr_enabled` 两个布尔值，当前订阅入口会同时开关二者，不能表达“已申请 / 已批准 / 已拒绝 / 被撤销”。
4. 派发日报当前会自动订阅，但只授予公开任务只读能力，不授予私人任务访问权；2.0 仍需定义引入审批/关闭策略后是否继续自动建立订阅关系。
5. 缺少订阅行为审计与通知。

### 5.2 设计目标

- 订阅是针对“人 + 内容类型”的授权关系。
- 日报和 OKR 可以独立授权。
- 订阅者只能读取被授权的内容明细。
- 个人主页评分摘要不受订阅限制。
- 被订阅人应能控制自己的日报/OKR 是否允许被订阅。
- 管理员不默认绕过个人内容隐私，除非后续明确做“组织监管视图”。

### 5.3 可见性策略

为用户增加订阅可见性设置，日报与 OKR 分开配置：

| 策略 | 说明 |
|---|---|
| `open` | 允许其他 active 员工直接订阅，保持当前 1.0 行为 |
| `approval_required` | 其他员工发起申请，被订阅人批准后才可读 |
| `closed` | 不允许主动订阅；已批准关系可按用户选择保留或失效 |

建议默认值：

```text
daily_visibility = open
okr_visibility = open
```

这样 2.0 上线不会破坏 1.0 的既有订阅体验，再允许用户逐步收紧。

### 5.4 数据模型

建议新增或调整：

#### user_privacy_settings

| 字段 | 说明 |
|---|---|
| `user_id` | 用户 |
| `daily_visibility` | `open/approval_required/closed` |
| `okr_visibility` | `open/approval_required/closed` |
| `created_at/updated_at` | 审计时间 |

#### subscriptions

在现有表基础上扩展：

| 字段 | 说明 |
|---|---|
| `daily_status` | `none/pending/approved/rejected/revoked` |
| `okr_status` | `none/pending/approved/rejected/revoked` |
| `daily_enabled` | 可保留作兼容字段，后续由 `daily_status == approved` 派生 |
| `okr_enabled` | 可保留作兼容字段，后续由 `okr_status == approved` 派生 |
| `approved_by` | 最近一次批准人 |
| `approved_at` | 最近一次批准时间 |
| `rejected_at` | 最近一次拒绝时间 |
| `revoked_at` | 最近一次撤销时间 |

#### subscription_audit_logs

| 字段 | 说明 |
|---|---|
| `actor_id` | 操作人 |
| `subscriber_id` | 订阅人 |
| `target_user_id` | 被订阅人 |
| `content_type` | `daily/okr` |
| `action` | `request/approve/reject/revoke/cancel/auto_grant` |
| `reason` | 可选原因 |
| `created_at` | 操作时间 |

### 5.5 权限规则

#### 订阅日报

| 状态 | 是否能读取日报 |
|---|---:|
| `daily_status=approved` | 是 |
| `daily_status=pending` | 否 |
| `daily_status=rejected` | 否 |
| `daily_status=revoked` | 否 |
| 无订阅关系 | 否 |

#### 订阅 OKR

| 状态 | 是否能读取 OKR/月报 |
|---|---:|
| `okr_status=approved` | 是 |
| `okr_status=pending` | 否 |
| `okr_status=rejected` | 否 |
| `okr_status=revoked` | 否 |
| 无订阅关系 | 否 |

### 5.6 派发自动订阅规则

1. 如果被派发人的 `daily_visibility=open`，派发发起人自动获得该被派发人的日报订阅。
2. 如果被派发人的 `daily_visibility=approval_required`，派发后创建一条 `pending` 订阅申请，并通知被派发人审批。
3. 如果被派发人的 `daily_visibility=closed`，不自动订阅；派发发起人仍只保留 `assigned_by` 审计记录。
4. 派发给自己不建立自订阅关系，保持 1.0 规则。

### 5.7 前端页面

个人主页：

- 他人主页的订阅按钮根据对方隐私策略显示：
  - `open`：显示“订阅”。
  - `approval_required`：显示“申请订阅”。
  - `closed`：显示“暂不可订阅”。
- 已申请未审批时显示“待对方确认”。
- 已批准时显示“已订阅”。

新增个人隐私设置入口：

- 可放在个人主页签名区域附近，或后续统一放到个人设置页。
- 配置日报订阅可见性。
- 配置 OKR 订阅可见性。
- 查看待我审批的订阅申请。

订阅页：

- 已订阅列表只展示 approved。
- 候选列表展示申请状态。
- pending/rejected/revoked 状态有明确文案，不允许进入明细。

### 5.8 API

建议新增：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/privacy/me` | 获取我的订阅隐私设置 |
| PATCH | `/api/v1/privacy/me` | 修改我的日报/OKR 订阅可见性 |
| GET | `/api/v1/subscriptions/requests` | 获取待我审批的订阅申请 |
| POST | `/api/v1/subscriptions/{id}/approve` | 批准订阅申请 |
| POST | `/api/v1/subscriptions/{id}/reject` | 拒绝订阅申请 |
| POST | `/api/v1/subscriptions/{id}/revoke` | 撤销已批准订阅 |

现有订阅接口调整：

- `POST /subscriptions/daily/{target_user_id}`：
  - `open`：直接 approved。
  - `approval_required`：创建 pending。
  - `closed`：返回 403。
- `GET /subscriptions/daily/{target_user_id}/report`：仅 approved 可读。
- OKR 对应接口同理。

### 5.9 验收

- `approval_required` 下，申请人无法在批准前读取日报/OKR。
- `closed` 下，普通员工无法发起订阅。
- 被订阅人可以批准、拒绝、撤销。
- 撤销后订阅者立即不能读取明细。
- 已批准订阅不影响个人主页评分摘要的可见性。
- 所有订阅状态变化写入审计日志。
- 派发自动订阅遵守被派发人的日报隐私策略。

---

## 6. 订阅聚合流

### 6.1 目标

把订阅日报和订阅 OKR 的更新整理成一个更像“关注动态”的页面，减少用户逐个点人的成本。

### 6.2 范围

- 聚合我已 approved 的日报订阅。
- 聚合我已 approved 的 OKR 订阅。
- 支持按人、内容类型、日期/月度筛选。
- 只展示摘要卡片，点击进入现有只读详情页。

### 6.3 不做

- 不在聚合流里提供编辑。
- 不绕过订阅隐私状态。
- 不显示未获授权的日报/OKR 明细。

---

## 7. 后台补齐项

### 7.1 飞书同步日志列表

状态：1.0 已落地（2026-07-15）。

已实现：

- 后台页 `/admin/sync-history`（`ContactSyncHistory`）。
- 接口 `GET /api/v1/admin/feishu/sync-logs`。
- 展示开始时间、状态、新增/更新/禁用/跳过、失败原因。

仍可增强（非 P0）：

- 操作人字段。
- 更细的筛选与分页。

### 7.2 企业设置审计日志

企业设置 1.0 已能修改企业名称、Logo、页脚，但缺审计。

需要补：

- 修改企业名称记录。
- 上传/替换 Logo 记录。
- 修改页脚记录。
- 记录操作者、时间、字段、旧值摘要、新值摘要。

---

## 8. 通知中心

2.0 通知中心先服务于订阅与协作，不做复杂 IM 推送。

候选通知类型：

| 类型 | 接收人 | 场景 |
|---|---|---|
| `subscription.requested` | 被订阅人 | 有人申请订阅我的日报/OKR |
| `subscription.approved` | 申请人 | 订阅申请通过 |
| `subscription.rejected` | 申请人 | 订阅申请被拒绝 |
| `subscription.revoked` | 订阅者 | 对方撤销了订阅授权 |
| `daily.assigned` | 被派发人 | 有日报任务派发给我 |
| `daily.score_ready` | 本人 | 日报评分生成完成 |
| `okr.review_ready` | 本人 | OKR 点评生成完成 |

---

## 9. 灵感清单

1.0 导航里曾预留灵感清单，但未落地。

2.0 可做成轻量个人模块：

- 快速记录灵感。
- 支持标签。
- 支持转为日报任务。
- 支持转为 OKR 草稿或月报素材。
- 默认仅本人可见。
知识库，智能纪要飞书导入

---

## 10. 暂缓项

以下能力不进入 2.0 第一轮主线：

- AI 大脑聊天。
- AI 智能体。
- Claude 额度授权。
- 多模型额度统计。
- 组织监管视图。
- 邮件/飞书 IM 主动推送。
