# 企业管理工作台 · 项目进度与完成度

版本：v1.14 · v2.0 规划补充 · 更新日期：2026-07-20
配套主文档：`企业管理工作台开发文档.md`
配套 2.0 文档：`企业管理工作台2.0开发文档.md`
配套配置指引：`飞书应用接入与配置指引.md`

> 本文是当前实现进度视图：记录已完成部分、正在联调的任务、以及明确后续项。完整业务逻辑仍以主开发文档为准；如历史章节与本文冲突，以本文最新实现结论为准。

---

## 1. 技术栈与结构

- **后端**：FastAPI + SQLAlchemy 2.0 + Alembic + SQLite（`backend/`）。分层：`api/v1`、`services`、`schemas`、`models`、`utils`。
- **前端**：React 18 + TypeScript + Vite + React Router + TanStack Query + Zustand + Tailwind v4 + dayjs（`frontend/`）。
- **认证**：飞书扫码 OAuth → 本系统 JWT + httpOnly Cookie + CSRF 双提交 + `token_version` 会话失效。

---

## 2. 已完成

### 2.1 认证与花名册准入

- 飞书扫码 OAuth 登录链路保留。
- 首次部署要求配置 `OWNER_FEISHU_UNION_ID`。
- 空库首次登录仅允许配置的 owner union id 创建 owner。
- 非空库登录改为**花名册门控**：飞书 `union_id` 必须命中本地 `users`，未命中直接拒绝。
- 禁用用户拒绝登录；禁用时 `token_version+1` 使旧会话失效。
- 取消“首登自动建普通账号”和“首个登录者自动 owner”。
- 去除 `OWNER_EMAIL` owner 兜底方向，owner 以 union id 为准。

### 2.2 飞书通讯录同步

- 新增应用身份 `tenant_access_token` 获取与进程内缓存。
- 新增飞书部门/用户分页拉取服务，端点通过 `.env` 可配置。
- 新增通讯录同步服务：
  - 部门逐层 BFS 拉取（`/departments/0/children`，不带 `fetch_child`），父 id = 当前查询部门，重建三层部门树。（v1.5 修复：`fetch_child=true` 递归返回扁平列表、`parent_department_id` 恒 null，无法建树。）
  - 用户逐部门查询（飞书按部门只返回直属成员），累加每人的全部部门；`department_id` 择优：最外层（depth 最小）优先，同层按 `.env DEPARTMENT_PRIORITY` 名称顺序，均未列举按发现顺序。（v1.5 修复：飞书 `/users` 精简返回不含 `department_ids`，原逻辑归属恒空。）
  - 部门按 `feishu_department_id` upsert（不过滤软删，命中软删行则复活，避开 unique 索引冲突）。
  - 用户按 `feishu_union_id` upsert。
  - 新用户默认 `member`，owner union id 命中则为 `owner`。
  - 已存在用户同步姓名、邮箱、头像、飞书 ID、部门、同步时间。
  - 本地 active 但本次通讯录缺失的非 owner 用户置为 disabled。
  - owner 不被同步降级、禁用或删除。
- 岗位（position）**不纳入同步**（飞书无权限返回 job_title）。部门名称需飞书应用具备 `contact:department.base:readonly` 等权限并发版后才返回。
- 新增 `contact_sync_logs` 记录同步结果；同步异常先 `rollback` 再写失败日志。
- 新增接口：`POST /api/v1/admin/feishu/sync-contacts`。
- 人员后台新增“从飞书同步通讯录”按钮和统计回显。
- 手工预建员工路径已移除。

### 2.3 系统级权限

- 功能权限用于控制前台模块是否可用，owner 也按权限行判断：
  - `feature:daily`
  - `feature:traffic`
  - `feature:okr`
  - `feature:group`
- 后台权限由 owner 绕过，普通员工需显式授予：
  - `admin:employee`
  - `admin:department`
  - `admin:settings`
  - `admin:ai`
- 新同步员工默认获得全部 `feature:*`，后台权限默认不授予。
- 订阅日报/OKR 与对应父功能使用同一权限门控，不能通过订阅接口绕过功能关闭。
- /admin 仍由 owner 或任一后台权限点门控。
- （v1.13）废除可分配的 ole=admin：系统角色仅保留 owner / member；访问控制完全由 UserPermission 行 + owner 对 dmin:* 绕过决定，员工更新接口不再接受 ole。
- （v1.5）人员管理入口从导航 tab 移到顶栏右上角（通知铃铛与用户 pill 之间，`Users` 图标），由 `admin:employee` 门控；路由门控不变。

### 2.3.1 部门与人员组（v1.5，替代原“岗位”）

- 岗位（position）已彻底移除：`Position` 模型、`users.position_id`、相关 schema/service/api CRUD、前端类型与岗位列全部删除。
- 新增“人员组”：`groups` + `group_members` 两表；组为手动管理实体，成员可完全自由编辑。
- 组来源标记 `source`（`manual` 手工建 / `department` 从部门导入）；“从部门导入”是一次性快照，导入后与部门解耦，部门增减人不影响已建组。
- 独立 /groups 页面：建组、改名、删组、成员编辑（复用选人弹窗）、按部门一键导入，由 feature:group 门控。
- （v1.13）人员组导航入口已加入顶栏（`feature:group` 门控）；未实现模块（AI 大脑/灵感库/文档/知识库/多维表格/智能体设置）顶栏置灰不可点。
- 接口：`/api/v1/groups` CRUD + `/groups/{id}/members` + `/groups/from-department`；`/api/v1/users/groups` 供派发/成员选择器展开成成员。

### 2.4 工作日报

- 日期周条 + 原生日期选择；支持独立日/周/月视图，点击周一至周日回到日视图。今日始终使用当前主题强调色高亮，并与普通选中日期区分。
- 工作清单：增删改、标记完成、5 分钟粒度时间。
- 重复标记：今日 / 每日 / 每周。每日任务顺延到下一工作日，每周任务顺延 7 天且落在周末时后跳；创建时预生成一个未来实例，单实例调度器每天 00:05 幂等补齐下一实例。
- 派发：派发是操作，不是身份；创建时选择派发目标会直接把任务创建到被派发人日报下，任务所有者为被派发人。
- （v1.5）派发目标支持“人员或人员组”：`DailyTaskCreate.assigned_to_ids` 改为多值，前端把人员组展开成成员 id 提交，后端为每个派发目标各建一份任务；目标可以包含自己。
- （v1.6）派发日报任务时，派发发起人会自动订阅所有被派发人的日报，便于后续通过订阅日报只读跟进。
- 协作者：`daily_task_collaborators`。
- 日报任务资源权限已落地：
  - owner：查看、编辑内容、删除、标记完成、管理协作者/派发。
  - collaborator：查看、标记完成；不能编辑内容、删除或管理成员。
  - follower：由订阅日报产生，只读，不能编辑/删除/标记完成/管理协作者。
  - 其他人：不可见。
- 私人是任务级可见性：`is_private=true` 时 owner 与明确加入的 collaborator 仍可见，普通 follower/订阅者不可见；派发后的任务归被派发人所有。派发发起人不会因 `assigned_by` 或自动订阅获得私人任务查看权。同一天的公开任务仍可进入订阅日报。
- 问题与解决方案：增删改，TipTap 富文本 + 后端 HTML 白名单清洗。

### 2.4.1 订阅日报

- 新增 `subscriptions` 表，按 `subscriber_id + target_user_id` 管理订阅关系，当前启用 `daily_enabled`，预留 `okr_enabled`。
- 新增订阅日报接口：
  - `GET /api/v1/subscriptions/daily`
  - `GET /api/v1/subscriptions/daily/candidates`
  - `POST /api/v1/subscriptions/daily/{target_user_id}`
  - `DELETE /api/v1/subscriptions/daily/{target_user_id}`
  - `GET /api/v1/subscriptions/daily/{target_user_id}/report?date=YYYY-MM-DD`
- 订阅规则：active 员工可订阅其他 active 员工；不能订阅自己；取消订阅后不能再读取对方日报。
- 派发任务会自动建立或恢复发起人到被派发人的日报订阅关系。
- 订阅日报返回 `follower` 视图：工作清单与问题解决方案只读，任务所有 `can_*` 写能力为 false；任务列表在服务端过滤 `is_private=true` 的私人任务，不能通过订阅接口、AI 上下文或个人主页日报日历旁路读取。
- 前端新增 `/subscription/daily` 页面：已订阅同事列表、员工搜索订阅、按日期查看订阅日报、取消订阅。

### 2.4.2 个人主页（v1.7）

- 新增个人主页接口：
  - `GET /api/v1/people/me?month=YYYY-MM`
  - `GET /api/v1/people/{user_id}?month=YYYY-MM`
  - `POST /api/v1/people/{user_id}/subscribe`
  - `DELETE /api/v1/people/{user_id}/subscribe`
  - `POST /api/v1/people/me/avatar`
- 个人主页支持本人点击头像上传本地头像，保存到 `/uploads/avatars/`；飞书登录同步与通讯录同步会保留本地上传头像，避免被自动覆盖。
- 个人主页不展示日报任务详情或问题详情，这些内容仍只在订阅日报视图查看。
- AI 日报评分与 OKR 点评作为主页核心区域；有生成记录时展示真实结果，无记录时展示空态，且不要求订阅即可查看。
- 主页订阅按钮是“二合一”动作：同时开启/关闭 `daily_enabled` 与 `okr_enabled`。
- 新增当月日报记录日历：仅展示是否有日报记录；周日开头小日历，工作日已填为蓝点，工作日未填为红点，未来日期与非工作日不显示点。
- 新增本月概览：日报存在数量、日报缺少数量、工作清单平均完成率；完成率仅统计已结束工作日，今日在 20:00 后纳入。
- 新增粉丝/关注统计：粉丝为订阅该用户的人数；关注为该用户订阅的人数，同一目标用户不因日报/OKR 双开重复计数。
- 前端新增 `/people/me`、`/people/:userId`，并兼容 `/profile -> /people/me`；右上角用户 pill 可进入我的主页。
- 个人主页 UI 调整：订阅按钮放在头像信息区右侧，自己的主页不显示；部门和最近登录并入身份行，个性签名独立占一整行。
- 个性签名已落库到 `users.profile_signature`，本人可在个人主页直接编辑；他人只读。
- 最近登录时间前端按 `Asia/Shanghai`（UTC+8）格式化展示。
- 翻阅月份时使用上一份 profile 数据占位，仅日历和本月概览显示更新状态，避免整页刷新。
- 人员展示入口新增头像/姓名跳转个人主页：订阅日报的已订阅列表、订阅候选列表、订阅日报头部、日报任务协作者头像、后台员工列表均可进入 `/people/{user_id}`；选人弹窗保持选择行为，不做跳转。

### 2.5 红绿灯周指标（v1.8 重构为长期滚动 + 周目标达标）

- 指标不再绑定月份，长期存在；不设起点/月目标/累计方式，只设一个**统一周目标**。
- 时间列改为**最近 5 周滚动窗口**：每周一自动多出刚结束那周待填，向前整页翻 5 周看历史，可「回到最近」。
- 每周状态二元自动判定：实际值达到周目标 → 绿(on_target)，否则 → 红(missed)，未填 → 灰(empty)。
- 达标方向 4 种：increase(≥)、decrease(≤)、equal(=)、range(区间 [min,max])。
- 派生列：**近 N 周平均**（窗口内已填值均值）；指标行状态汇总（任一已填周未达标即红，全达标绿，全空灰）。
- 值按 ISO 周（周一）存储，唯一键 `(metric_id, week_start)`；未填周不落行；未结束的周拒绝录入。
- 端点改名：`GET /traffic/weeks?end=&count=`、`GET /traffic/metrics?end=&count=`、`PATCH /traffic/metrics/{id}/values/{week_start}`。
- 导航 Tab：全部 / 我创建的 / 待我填写 / 我可查看。
- `traffic_metric_members.role` 已扩展为：
  - `manager`：前端文案“可管理”，可改指标元信息、成员、周值；不可删除。
  - `editor`：前端文案“可编辑”，仅可录入/修改周值。
  - `viewer`：前端文案“可查看”，只读。
- owner 天然最高权限，可查看、填写、修改指标、管理成员、删除指标。
- 删除指标仅 owner 可执行。
- （v1.5）三档成员选择器支持选“人员组”：前端把组展开成成员 id 并入对应角色，后端成员入参仍为 user_id 列表，无需改表。

---

## 3. 正在联调 / 待验证

- 飞书通讯录接口：`tenant_access_token`、部门树、用户归属择优均已用脱敏测试数据本地验证通过。
- 部门中文名依赖飞书 `contact:department.base:readonly` 权限并发版；已加权限，待重新同步页面核对。
- 空库 owner bootstrap 真实扫码验证。
- 同步后普通员工扫码登录验证。
- 通讯录删除/离职后禁用与 `token_version+1` 验证。
- （v1.10）工程清债：前端 `Permission` 类型补齐 `admin:settings`；上传的企业 Logo/个人头像改为运行时数据并加入 Git 忽略；前后端 README 已同步到当前模块范围。
- （v1.5）页面回归：人员管理岗位列消失、部门与人员组 tab 建组/导入、日报与红绿灯选人时选人员组、顶栏人员管理图标就位。
- （v1.8）红绿灯页面回归：周窗口翻页与「回到最近」、4 方向录入/达标判色、近N周平均、每周一新增待填列、区间达标；后端服务层已本地 smoke test 通过。
- （v1.9）OKR 页面回归：按月切换、添加目标（含多个 KR）、KR 当前值行内更新与进度重算、KR 完整编辑/删除、目标编辑/删除、月报两板块富文本编辑；后端服务层已本地 smoke test 通过。

---

### 2.6 OKR 月度目标（v1.9）

- 数据模型：`okr_objectives`（按月 `YYYY-MM`，去 company_id）、`okr_key_results`、`monthly_report_sections`。
- 目标与 KR 均归属个人，仅本人可增删改自己的 OKR/月报。
- 进度计算（服务端）：KR 进度 = `(current-start)/(target-start)`，clamp 到 [0,100]，target=start 时按是否达标取 100/0；Objective 进度 = `Σ(KR进度×weight)/Σweight`（权重归一化），无权重时退化为等权平均。
- KR 状态 `normal/risk/done` 手动选择；权重合计非 100% 前端仅警示不拦截。
- **不做红绿灯联动**：KR `current_value` 纯手动录入，未建 `linked_metric_id`。
- 月报：每月首次访问自动补 `performance`（业绩相关）/`innovation`（本月创新）两板块空态；富文本复用 TipTap + 后端 `sanitize_html` 白名单清洗，存 HTML + JSON 字符串。
- AI 点评 / 月度质量评分已接入真实 Provider；未启用或无生成记录时展示空态。
- 接口：`GET /okr?month=`、`POST /okr/objectives`、`PATCH|DELETE /okr/objectives/{id}`、`POST /okr/objectives/{id}/key-results`、`PATCH|DELETE /okr/key-results/{id}`、`POST /okr/review/generate`、`PATCH /okr/monthly-report/sections/{id}`。
- 前端：`/okr` 路由（普通 active 员工可进，无独立权限点，与日报/红绿灯一致），导航新增「OKR」tab。
- 页面排版：第一行左「月份切换 + 说明」、右「OKR / 月报」切换；第二行「本月 OKR 均分」；下接目标列（KR 内嵌）；底部展示 AI 点评或未生成空态。

### 2.6.1 订阅 OKR（v1.9）

- 复用既有 `subscriptions` 表的 `okr_enabled`，无需新增迁移；订阅规则与订阅日报对称（active 员工互订、不能订阅自己、取消后不可读）。
- 取消订阅镜像日报逻辑：置 `okr_enabled=false`，若 `daily_enabled` 也为 false 则软删该订阅行。
- 只读视图：`OkrService.get_month_readonly` 读目标他人月度 OKR（不惰性补月报板块，仅返回对方已建板块）；进度等派生值服务端同款计算。
- 接口：`GET /subscriptions/okr`、`GET /subscriptions/okr/candidates`、`POST|DELETE /subscriptions/okr/{target_user_id}`、`GET /subscriptions/okr/{target_user_id}/report?month=`。
- 前端 `/subscription/okr` 页面：左侧已订阅列表 + 搜索订阅，右侧月份切换 + 只读目标/KR 进度 + 只读月报；导航新增「订阅 OKR」tab。
- 个人主页「二合一」订阅按钮已同时开关 `daily_enabled` 与 `okr_enabled`，与订阅 OKR 联通。

### 2.4.3 日报页面排版（v1.9）

- 从上到下：日期选择 → 今日建议 → 工作清单 → 问题与解决方案 → AI 日报与周评分；评分区并列展示今日、昨日、上一完整自然周得分，功能关闭或无结果时显示对应空态。
- 日/周/月视图共用日期导航：日视图展示完整编辑区域，周/月视图按时间顺序汇总任务；点击周/月卡片或顶部具体日期回到对应日视图。
- 工作清单取消拖拽和显式编辑按钮，默认按任务时间排序；owner 双击任务进入完整编辑，可同时修改时间、内容、标注、频率、协作人、派发目标和私人状态。关联文档暂时置灰不可用。

### 2.4.4 主题与界面设置（v1.14）

- 顶栏主题入口支持五种强调色、三种字体和三档字体/卡片大小，设置保存在当前浏览器 `localStorage`。
- 主题强调色已覆盖日报、OKR、红绿灯、日报/OKR 订阅、人员主页、人员组、企业设置和后台管理中的导航/Tab 激活态、选择态、输入焦点、普通操作按钮、可点击文字、功能图标及进度条；共享头像、用户链接和富文本编辑器也统一读取主题变量。
- 红绿灯红/黄/绿、成功、警告、错误、AI 建议分类、协作身份、人员日报存在状态、通讯录同步运行状态和品牌 Logo 保持固定语义/品牌色，避免主题切换改变业务含义。
- 字号档位同步调整日报周/月卡片和应用顶栏高度；紧凑/标准/舒展模式分别使用适配高度，避免根字号变化造成导航裁切或内容重叠。
- 日期卡、视图卡、日报标题栏、任务行和评分 Tab 使用随根字号缩放的 `rem` 尺寸；激活线必须定位在所属导航/Tab 内部，不得使用负 `bottom` 越过边框。共享按钮与链接的键盘焦点轮廓统一使用主题色，避免字体或尺寸切换后出现系统色高亮和位置跳动。
- 后续新增业务页面不得用固定 `blue-*` 表示普通交互强调色，必须读取 `--theme-accent`、`--theme-accent-hover`、`--theme-accent-soft`、`--theme-accent-ring` 或 `--theme-icon-color`；只有在颜色本身承载业务状态或品牌含义时才允许保留固定色。

---

### 2.7 AI 能力（v1.11）

- **Provider 抽象**：`services/ai/`（Provider 协议 + OpenAI Chat/Responses + Anthropic Messages HTTP 实现 + 工厂）。未启用/未配置返回「未启用」空态；真实请求或返回解析失败返回可重试错误，不影响其他业务。
- **密钥加密**：`utils/crypto.py` 用 `jwt_secret` 派生 Fernet key 对称加解密；仅存密文，API 回显脱敏 `sk-****abcd`。
- **数据模型**（迁移 0010、0016～0019）：`ai_provider_configs`（单行）、`ai_feature_flags`（单行）、`prompt_configs`（系统级单套/类型）、`ai_tasks`（审计+状态）、`daily_scores`、`weekly_scores`、`daily_suggestions`、`okr_reviews`、`monthly_report_scores`；月报评分独立保存四维分、存疑点与建议。
- **日报 AI 评分**：总分+等级+分差+三维度（重要性40/质量30/饱和度30）+趋势+一句话点评+OKR外高价值+管理者提示+OKR清晰度警告。`GET /daily/scores`、`POST /daily/scores/generate`。
- **周工作表现评分**：按锚点日期取上一完整周（周一至周日）的全部日报及问题方案综合评分，不平均每日分；维度为产出与结果40、问题方案质量30、工作饱和度30。`GET /daily/weekly-score`、`POST /daily/weekly-score/generate`。
- **今日建议**：采用「今日行动建议官」System 提示词，按当前用户、近 30 天全公司日报/问题方案/红绿灯备注/月报文档、本人 OKR/今日清单、历史及本次补充三层组装数据；输出 red/amber/blue/green、真实归因、关联来源和 needs_info 快捷追问。底部输入可带补充信息重生成，补充会从成功任务历史回灌；接受即建 `source=suggestion` 的 DailyTask。
- **OKR 整套质量评分**：`GET /okr/review`、`POST /okr/review/generate` 接真实 Provider，只输入 O/KR 方向、描述、目标量和权重，不输入完成率、当前值、状态、月报或日报；按 O 的价值40、KR 支撑度30、工作饱和度30评分，写入 `okr_reviews`，不再改写各 Objective 的 `ai_comment/score`。
- **月报评分**：展示在 OKR「月报」Tab，聚合当月日报统计、每日清单与问题方案原文、本月 OKR 进度、各月报栏目；按复盘深度50、饱和度20、真实性15、业绩目标15评分，并输出轻度存疑点和 2～3 条执行建议。`GET /okr/monthly-report/score`、`POST /okr/monthly-report/score/generate`。
- **同步生成**：点击即 httpx 调 Provider 等返回；每次落 `ai_tasks`（succeeded/failed）。`GET /ai/tasks[/{id}]` 本人查询。
- **输出校验与截断恢复**：Provider JSON 在任务成功前按任务类型通过 Pydantic 校验；日报、周分和 OKR 质量分必须等于各自维度分之和并符合固定上限。首次 JSON 截断、结构不完整或字段校验失败时自动要求模型严格按 Schema 重发一次；仍失败才写入 failed，且不覆盖旧评分。
- **定时任务**（APScheduler BackgroundScheduler，`main.py` lifespan 起停）：每天 07:50 全员今日建议、17:00/23:50 全员日报评分、每周一 00:10 上一完整周评分、月末 23:30 OKR 质量评分、23:40 月报评分、03:00 清理 30 天前 ai_tasks；受 `scheduler_enabled` + 各功能开关门控。
- **后台 AI 配置**（`admin:ai` 门控，`/admin/ai/*`）：Provider 表单、功能开关、系统级提示词模板（日报评分/周表现评分/今日建议/OKR 质量/月报评分五类型 Tab + 可选数据变量 + 版本 + 恢复默认）。
- **个人主页评分**：`people_service` 读真实最新 `daily_scores`/`okr_reviews`。
- 提示词粒度：系统级单套/类型（岗位/组织/个人级不做，岗位已 v1.5 移除）。
- 后端 pytest（mock Provider）：service 编排 + API 全链路（含密钥加密/脱敏、JSON 截断重试、周评分边界、今日建议分层上下文、OKR 无进度质量评分、月报整月聚合）44 项通过；前端 tsc+build 与生产依赖审计通过。

---

## 4. 占位 / 未启用

- AI 大脑聊天 `/chat`、AI 智能体、Claude 额度授权：本轮未做（`ai_agents`/`chat_*` 表未建）；顶栏与后台入口已置灰。
- 通知中心、灵感清单、文档、知识库、多维表格：导航置灰不可点。
- OKR 重要度 `o1/o2/o3` 与拖拽排序：当前未实现，已纳入 2.0 文档。
- 订阅权限隐私设计：当前仅为主动订阅 + 只读授权模型，缺被订阅人审批、关闭、撤销、审计等隐私控制，已纳入 2.0 文档。
- 个人主页评分隐私：当前已满足“仅展示分数和评价，不展示日报/OKR 明细”，2.0 作为非回归约束保留。

---

## 5. 数据库迁移

- `20260706_0001`：MVP 基础表。
- `20260708_0002`：`daily_tasks.assigned_by`、`daily_task_collaborators`、`traffic_metric_members`。
- `20260708_0003`：飞书通讯录同步字段、`contact_sync_logs`、旧红绿灯 `editor` 成员迁移为 `manager`。
- `20260709_0004`（v1.5）：drop `positions` 表与 `users.position_id`；新增 `groups`、`group_members`。
- `20260709_0005`：新增 `subscriptions` 表，落地订阅日报 follower 只读视图。
- `20260709_0007`：新增 `users.profile_signature`，用于个人主页本地个性签名。
- `20260709_0006`（v1.8）：红绿灯重构——drop 重建 `traffic_metrics`/`traffic_metric_values`/`traffic_metric_members`；指标去 month/start/target/agg_mode，加 `weekly_target`；值按 `week_start` 存（唯一键 `metric_id+week_start`）。旧红绿灯数据丢弃。
- `20260709_0008`：新增 `company_settings` 单行表，保存企业名称、Logo URL、页脚文案。
- `20260709_0009`（v1.9）：新增 OKR 三表 `okr_objectives`、`okr_key_results`、`monthly_report_sections`（月报板块唯一键 `user_id+month+section_key`）。
- `20260710_0010`（v1.11）：AI 七表 `ai_provider_configs`、`ai_feature_flags`、`prompt_configs`、`ai_tasks`、`daily_scores`、`daily_suggestions`、`okr_reviews`。`daily_scores`/`okr_reviews` 软删条件唯一键。
- `20260710_0011`：提示词配置清理，版本改为可编辑字符串。
- `20260710_0012`：前台功能权限与后台高级权限拆分，并为 active 用户回填默认功能权限。
- `20260710_0013`：提示词可选数据变量落库。
- `20260710_0014`：日报任务员工标注 + AI 用户长期记忆。
- `20260710_0015`：补齐 `daily_tasks.assigned_by` 数据库外键。
- `20260710_0016`：新增 `weekly_scores`，按员工 + 周起始日保存上一完整自然周 AI 综合评分。
- `20260710_0017`：今日建议升级为结构化行动建议，增加类型、关联来源、追问状态与快捷选项，并重置为新版系统提示词和八类数据变量。
- `20260710_0018`：OKR 点评升级为整套 OKR 质量评分，新增等级、亮点、温和优化建议和日报评分联动提示，并禁止将完成进度作为评分输入。
- `20260710_0019`：新增 `monthly_report_scores`，保存月报四维评分、存疑点和可执行建议。
- 20260710_0020：新增日报重复系列标识与 active 唯一索引，清理 Objective 旧评分字段，并补齐用户权限、AI 用户记忆的 active 唯一约束。
- 20260715_0021：新增 OKR KR 进展历史表 okr_key_result_progress 与评论表 okr_comments。
- 20260715_0022：历史 role=admin 归一为 member（权限改由 permission 行控制）。

升级命令：

```powershell
cd backend
..\.venv\Scripts\python -m alembic upgrade head
```

---

## 6. 当前完成度估算

| 模块 | 完成度 | 说明 |
|---|---:|---|
| 认证与会话 | 85% | OAuth state 已绑定发起浏览器；refresh 滑动轮换、禁用吊销完成；需真实飞书扫码回归 |
| 花名册准入 | 85% | 代码完成；需真实 owner bootstrap 与拒登验证 |
| 飞书通讯录同步 | 85% | 部门树/用户归属/择优和事务回滚已修复并本地验证；部门中文名仍依赖飞书通讯录权限 |
| 人员后台 | 90% | 同步按钮、后台权限、部门管理、同步日志列表完成；人员组迁至工作台 `/groups` |
| 工作日报 | 95% | CRUD/协作/派发/资源权限与每日/每周重复任务完成；重复链创建、同步、停止和幂等补链已有回归测试 |
| 红绿灯 | 85% | 长期滚动、4 方向、近N周平均、成员角色/权限完成；成员移除后回添已修复；需页面回归 |
| 订阅日报 | 80% | 订阅/取消/按人按日只读查看完成；缺订阅聚合流与通知 |
| 个人主页 | 90% | 头像信息区、粉丝/关注、签名、AI 评分、二合一订阅、日报月历和概览完成；基础资料以飞书同步为准 |
| 企业设置 | 75% | `admin:settings`、企业名称、Logo 上传、页脚文案完成；已影响登录页、顶栏与页脚；缺审计日志与更多品牌项 |
| OKR | 82% | 目标/KR CRUD、加权进度、月报富文本和 AI 点评完成；需真实 Provider 与页面回归 |
| 订阅 OKR | 80% | 订阅/取消/按人按月只读查看 OKR + 月报完成；与二合一订阅联通；需页面回归 |
| AI | 78% | 日报/周评分、建议、OKR点评、用户记忆、Provider、密钥、定时任务、后台配置、结构校验及 JSON 截断重试完成；缺 AI 大脑聊天/智能体/额度及真实 Provider 联调 |

---

## 7. 本地联调

1. 后端：`cd backend; ..\.venv\Scripts\python -m uvicorn app.main:app --reload`
2. 前端：`cd frontend; npm run dev`
3. 登录前在 `backend/.env` 配置飞书应用与 `OWNER_FEISHU_UNION_ID`。
4. 空库先由 owner 扫码登录，再到人员管理执行“从飞书同步通讯录”。

---

## 8. 2.0 开发文档索引

2.0 文档只记录 1.0 未实现与新增功能，当前已整理到 `企业管理工作台2.0开发文档.md`。

本次检查结论：

| 项目 | 检查结果 | 处理 |
|---|---|---|
| OKR 重要度 `o1/o2/o3`、按重要度排序、可拖动 | 未实现。后端仅有 `sort_order`，无 `importance` 字段；前端无拖拽排序 | 加入 2.0 P0 |
| 个人主页评分不得出现日报、OKR 明细 | 已实现。接口与前端只展示分数、摘要和生成时间 | 不列为开发项，作为 2.0 隐私非回归约束 |
| 订阅权限与隐私设计 | 未实现完整隐私模型。当前缺审批、关闭、撤销、状态流、审计 | 加入 2.0 P0 |
| 后台飞书同步日志列表 | 已实现。`ContactSyncHistory` + `/admin/sync-history` + 接口 | 移出 2.0 P0 |

2.0 当前 P0：

1. OKR 重要度与拖拽排序。
2. 订阅权限与隐私设计。
3. 订阅聚合流。
4. 企业设置审计日志。

已从 2.0 P0 移除（1.0 已落地）：

- 后台飞书同步日志列表（`/admin/sync-history` + `GET /admin/feishu/sync-logs`）。
