# 飞书应用接入与配置指引

面向：本平台部署 / 运维负责人。
目标：从零创建飞书自建应用，配好内嵌扫码登录，并让本平台跑通「扫码 → 回调 → 建号 → 会话」。

> 前置认知：本平台已废除自建密码，**唯一登录方式是飞书扫码**。飞书只负责"证明你是谁"，之后的会话（JWT + httpOnly Cookie + CSRF）由本系统自己签发和掌控。
>
> 🆕 **v1.3 准入变更**：登录改为**花名册门控**——先由管理员从飞书通讯录同步本企业人员，登录时以 `union_id` 比对花名册，**未命中直接拒绝**（不再首登自动建号）。因此除登录 scope 外，还需申请通讯录同步 scope（见 §3）。设计见主文档 §37。
>
> ⚠️ 本文所列飞书端点 / scope / SDK 版本，均以你操作时飞书开放平台控制台与官方文档为准。凡有出入，改 `.env` 配置即可，**无需改代码**（端点、scope、SDK 地址都做成了可配置项）。文末附「上线前必须核对」清单。

---

## 0. 你需要产出的 5 个值

配置完成后，你会把这几项填进后端 `backend/.env`：

| .env 键 | 含义 | 从哪来 |
|---|---|---|
| `FEISHU_APP_ID` | 应用 App ID（`cli_` 开头） | 控制台「凭证与基础信息」 |
| `FEISHU_APP_SECRET` | 应用 App Secret | 控制台「凭证与基础信息」 |
| `FEISHU_REDIRECT_URI` | 回调地址 | 你自己定，需与控制台白名单**逐字一致** |
| `FEISHU_OAUTH_SCOPE` | 申请的权限范围 | 控制台「权限管理」申请后填 |
| `OWNER_FEISHU_UNION_ID` | 初始管理员（可选） | 见 §6，也可留空用首登引导 |

---

## 1. 创建自建应用

1. 用**企业管理员**账号登录飞书开放平台：https://open.feishu.cn/ 。
2. 「开发者后台」→「创建企业自建应用」，填应用名称、图标、描述。
3. 创建后进入应用详情页，左侧「凭证与基础信息」可见 **App ID** 和 **App Secret** —— 这就是 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`。
4. App Secret 与数据库密码同等保密：只进 `.env`（已被 git 忽略），不要提交、不要贴到聊天/工单。

## 2. 配置重定向 URL（最容易踩坑）

授权成功后飞书会带着 `code&state` 跳回你的 `redirect_uri`，该地址必须提前在控制台登记进白名单，否则飞书拒绝跳转。

1. 控制台「安全设置」→「重定向 URL」（也叫 OAuth 回调 / 重定向域名）。
2. 添加你的回调地址，**与 `.env` 里的 `FEISHU_REDIRECT_URI` 逐字一致**（协议、域名、端口、路径、结尾斜杠都要一样）：
   - 本地开发：`http://localhost:5173/login/callback`
   - 生产：`https://你的域名/login/callback`
3. 本平台前端回调路由就是 `/login/callback`（见 [CallbackPage.tsx](frontend/src/features/auth/CallbackPage.tsx)），路径固定，你只需替换协议+域名。

> 常见报错：跳转后飞书报 `redirect_uri` 不在白名单 / mismatch → 十有八九是结尾斜杠或端口不一致。

## 3. 申请权限（scope）

平台登录只需要能读到用户基本信息（拿 `union_id / open_id / name / email / avatar`）。

1. 控制台「权限管理」→ 搜索并申请**读取用户基本信息**相关权限（如通讯录用户基础信息只读）。
2. 记下对应的 scope 键，填进 `.env` 的 `FEISHU_OAUTH_SCOPE`（空格分隔多个）。
   - 默认给的是 `contact:user.base:readonly`，**以你控制台实际可申请到的键为准**。
3. 若申请的权限需要管理员审批，走完审批流程后再联调。

**v1.3 通讯录同步额外权限**（用于「从飞书同步通讯录」按钮，应用身份 `tenant_access_token` 调用，非登录 scope）：

| 用途 | scope 键 |
|---|---|
| 获取企业信息 | `tenant:tenant:readonly` |
| 获取通讯录基本信息 | `contact:contact.base:readonly` |

这两项在控制台申请一次即可（本项目已申请）。同步端点默认值见 `.env.example` 的 `FEISHU_TENANT_TOKEN_PATH / FEISHU_CONTACT_DEPT_PATH / FEISHU_CONTACT_USER_PATH`，如官方端点变化改配置即可。

> 若登录时用户端出现 `20027` 报错，通常是 scope 没在后台申请或拼写不符。

**AI 大脑查宝额外权限**（应用 B 使用应用身份同步安全群，不能只配置登录和通讯录权限）：

| 用途 | scope 键 |
|---|---|
| 读取单聊、群组消息 | `im:message` 或 `im:message:readonly` |
| 读取群组中的全部消息 | `im:message.group_msg` |
| 获取群信息与群成员 | 按飞书控制台当前接口提示申请群信息、群成员只读权限 |
| 下载查宝返回的文件 | `im:resource` |

其中 `im:message.group_msg` 是读取群聊历史的必要补充权限。缺少它时，网页仍可能以用户身份把问题发送到群里，查宝也会在飞书群中正常回答，但应用 B 无法把群消息读回工作管理平台；飞书通常返回错误码 `230027`。申请或变更权限后，必须完成管理员审批、创建并发布新应用版本，再重启后端或等待同步任务自动重试。

应用 B 还必须开启机器人能力、加入目标安全群，且目标群不能启用会禁止历史消息 API 的保密模式。上线前应运行 `python scripts/verify_feishu_chat_phase0.py readonly`，确认输出中的 `members.ok` 和 `messages.ok` 都为 `true`，再开启 `FEISHU_CHAT_ENABLED=true`。

## 4. 开通「网页扫码登录」能力

内嵌扫码用的是飞书**扫码登录 JSSDK**（在你页面里渲染可嵌的二维码），不是把授权页塞 iframe（授权页带反劫持头，不能内嵌）。

1. 确认应用「安全设置 / 登录能力」里已开通网页/扫码登录相关能力（不同版本控制台位置略有差异）。
2. SDK 地址已在 `.env` 的 `FEISHU_QR_SDK_SRC` 预置了一个版本；若官方文档给的是别的版本号，替换该值即可。

## 5. 填写后端 `.env`

在 `backend/.env`（参考 `backend/.env.example`）填：

```dotenv
FEISHU_APP_ID=cli_你的AppID
FEISHU_APP_SECRET=你的AppSecret
FEISHU_REDIRECT_URI=https://你的域名/login/callback   # 必须与控制台白名单逐字一致
FEISHU_OAUTH_SCOPE=contact:user.base:readonly          # 以控制台实际申请到的为准
# 下面几项一般用默认即可，仅当飞书文档端点/SDK 版本变化时才改：
# FEISHU_AUTHORIZE_BASE=https://accounts.feishu.cn
# FEISHU_AUTHORIZE_PATH=/open-apis/authen/v1/authorize
# FEISHU_TOKEN_PATH=/open-apis/authen/v2/oauth/token
# FEISHU_USER_INFO_PATH=/open-apis/authen/v1/user_info
# FEISHU_QR_SDK_SRC=https://lf-package-cn.feishucdn.com/.../LarkSSOSDKWebQRCode-1.0.3.js
```

改完 `.env` **必须重启后端**（`--reload` 只监视 .py，不重读 .env）。

## 6. 设定 Owner（初始最高管理员）

纯飞书登录下，Owner 是唯一的初始管理员入口。

> 🆕 **v1.3 起，Owner 必须用 `OWNER_FEISHU_UNION_ID` 指定**——「首登即 owner」的兜底已取消（与花名册门控冲突：未在花名册内的人根本无法登录）。

- 指定 union_id（**推荐/必需**）：`OWNER_FEISHU_UNION_ID=on_xxx`（最稳，换邮箱/手机号不受影响）。同步/登录命中该 union_id 即 `role=owner`，且不被同步流程降级或禁用。
- 不再使用 `OWNER_EMAIL` 作为 owner 兜底；首次部署必须配置 `OWNER_FEISHU_UNION_ID`。

> 建议首次上线顺序：配好 `OWNER_FEISHU_UNION_ID` → 该 owner 先扫码登录（或先跑一次通讯录同步把自己纳入花名册）→ owner 在后台点「从飞书同步通讯录」导入全员 → 其余人即可登录。

## 7. 联调验证顺序

1. `.env` 填好 App ID/Secret/回调/scope，重启后端。
2. 起前端 `npm run dev`，打开 `/login`。
3. 页面应渲染出飞书二维码。
4. 用**已加入该企业**的飞书 App 扫码 → 确认授权 → 自动跳回 `/login/callback` → 进入 `/daily`。
5. 首个登录者应成为 owner（`GET /api/v1/auth/me` 里 `role: "owner"`）。

## 8. 上线前必须在控制台/文档核对的三项 ⚠️

以下三点我无法在离线环境替你验证，**必须以你飞书控制台和最新官方文档为准**：

| 项 | `.env` 键 | 核对什么 |
|---|---|---|
| Token 端点 | `FEISHU_TOKEN_PATH` | 换 `user_access_token` 的实际路径与版本（默认用 v2 `/authen/v2/oauth/token`）。若你的应用/文档用的是别的版本，改这里。 |
| 权限 scope | `FEISHU_OAUTH_SCOPE` | 控制台实际申请到的、能拉 `user_info` 的 scope 键，与此处逐字一致。 |
| 扫码 SDK 版本 | `FEISHU_QR_SDK_SRC` | 官方文档给出的 JSSDK 地址/版本号是否与预置值一致。 |

全部做成 `.env` 可配，就是为了让这三项在联调时"改配置不改代码"。

## 9. 安全要点

- `state` 由后端 HMAC 签名 + 10 分钟过期，`callback` 校验，防 OAuth CSRF。
- 不持久化飞书 `user_access_token`（本平台只用它拉一次 user_info 就丢弃）。
- 会话走本系统自签 JWT + httpOnly Cookie + CSRF + `token_version` 失效，飞书只负责"证明你是谁"。
- 被禁用（`status=disabled`）用户即使飞书授权成功也拒绝登录。
- 上线务必确认 `COOKIE_SECURE=true`（HTTPS 下）。

